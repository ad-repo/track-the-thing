# Desktop-specific environment defaults for the Track the Thing Tauri build.
# Copy this file to `.tourienv` and adjust the values as needed.

# Networking ------------------------------------------------------------------
# Host/port for the packaged FastAPI backend. Defaults intentionally avoid the
# docker-compose ports (8000/8001) so both stacks can run at the same time.
TAURI_BACKEND_HOST=127.0.0.1
TAURI_BACKEND_PORT=18765

# Data paths ------------------------------------------------------------------
# Base directory that holds the desktop-only SQLite database, uploads, and any
# extracted static assets. Use platform-appropriate paths (examples below).
#   macOS:   ~/Library/Application Support/TrackTheThingDesktop
#   Linux:   ~/.local/share/track-the-thing-desktop
#   Windows: %LOCALAPPDATA%\\TrackTheThingDesktop
TAURI_DESKTOP_DATA_DIR="~/Library/Application Support/TrackTheThingDesktop"
TAURI_DATABASE_PATH="${TAURI_DESKTOP_DATA_DIR}/ttt_desktop.db"
TAURI_UPLOADS_DIR="${TAURI_DESKTOP_DATA_DIR}/uploads"
TAURI_STATIC_DIR="${TAURI_DESKTOP_DATA_DIR}/static"

# Window behavior -------------------------------------------------------------
# Ratio of the available screen height that the main window should occupy on
# startup. The Rust bootstrapper will clamp this between 0.5 and 0.98.
TAURI_WINDOW_HEIGHT_RATIO=0.70
# Optional explicit width in logical pixels. Leave blank to let Tauri auto-size
# to (51% of screen width + 510px) as defined in lib.rs.
TAURI_WINDOW_WIDTH=
# Set to "true" to start maximized immediately.
TAURI_WINDOW_MAXIMIZED=false

# Splash screen ----------------------------------------------------------------
# Minimum number of milliseconds to keep the splash visible, even if the
# backend reports ready sooner.
TAURI_SPLASH_MIN_VISIBLE_MS=1200

# Backend launcher ------------------------------------------------------------
# Command that PyInstaller should freeze. In most cases this should be left as
# `python3 backend/desktop_launcher.py`, but you can supply a venv python path.
PYINSTALLER_ENTRYPOINT=python3 backend/desktop_launcher.py

# Logging ---------------------------------------------------------------------
# Location of the backend log file when launched from the desktop shell.
TAURI_BACKEND_LOG="${TAURI_DESKTOP_DATA_DIR}/logs/backend.log"


